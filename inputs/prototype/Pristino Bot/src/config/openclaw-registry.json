{
  "version": "1.0",
  "description": "OpenClaw Symlink Registry. Mapea skills externos al kernel de Pristino sin duplicación de código.",
  "skills": [
    {
      "namespace": "openclaw",
      "name": "document_intelligence",
      "type": "executable",
      "symlink_target": "../../../openclaw-skills/bin/doc_parser.js",
      "description": "Extrae texto puro de PDFs y DOCX.",
      "parameters": {
        "type": "object",
        "properties": {
          "filePath": { "type": "string", "description": "Ruta absoluta al documento" }
        },
        "required": ["filePath"]
      },
      "status": "pending_installation"
    },
    {
      "namespace": "openclaw",
      "name": "calendar_orchestrator",
      "type": "mcp_server",
      "symlink_target": "http://localhost:8080/mcp",
      "description": "Gestiona eventos del calendario.",
      "parameters": {
         "type": "object",
         "properties": {
            "action": { "type": "string" },
            "time": { "type": "string" }
         },
         "required": ["action"]
      },
      "status": "pending_installation"
    }
  ]
}
